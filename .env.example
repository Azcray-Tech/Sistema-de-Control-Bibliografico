PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ceela_biblioteca
DB_USER=root
DB_PASS=
DB_SSL=false
SESSION_SECRET=tu_secreto_aqui_cambialo

# Generate a secure SESSION_SECRET with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Set DB_SSL=true for TiDB Serverless / cloud MySQL with TLS required
